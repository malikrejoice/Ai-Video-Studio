# Copy this file to .env.local and replace with your actual values
PYTHON_AI_SERVER_URL=http://localhost:6000
NEXT_PUBLIC_API_BASE_URL=http://localhost:5000
# Set either the AnimateDiff command or root install path before running ai_server.py
# ANIMATEDIFF_CMD=python -m animatediff
# ANIMATEDIFF_ROOT=C:\path\to\AnimateDiff
# Set DEMO_MODE=1 only when you want the mock generator instead of real AnimateDiff

